load("@rules_cc//cc:defs.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "bundle_adjustment_util",
    hdrs = ["bundle_adjustment_util.h"],
    deps = [
        "//:symforce",
        "@eigen",
    ],
)